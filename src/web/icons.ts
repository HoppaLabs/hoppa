// Icons shared between the two editors.
//
// One file because they have to MATCH. The rubber was drawn on the character
// editor first, and the level editor's "clear" was the word "clear" next to a
// dark square -- the same act, in two places, looking nothing like each other.
// A copy in each page is a copy that drifts.

/**
 * The rubber, drawn as a rubber.
 *
 * A tool that takes things away, drawn as an absence, reads as a colour that
 * is MISSING rather than as a thing you can pick up and use. In the character
 * editor it sits on the chequer that means see-through; in the level editor it
 * sits on the floor tile it paints. Same tool, same drawing, both times.
 */
export const RUBBER_ICON =
  '<svg viewBox="0 0 24 24" aria-hidden="true">' +
  '<path d="M8.5 18.5H21"/>' +
  '<path d="M14.8 4.7 4.7 14.8a2 2 0 0 0 0 2.8l2.2 2.2a2 2 0 0 0 2.8 0L19.8 9.7a2 2 0 0 0 0-2.8' +
  'l-2.2-2.2a2 2 0 0 0-2.8 0Z"/>' +
  '<path d="M10 9.5 15 14.5"/>' +
  "</svg>";
